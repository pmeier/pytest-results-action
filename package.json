{
  "name": "pytest-results-actions",
  "version": "0.2.0",
  "description": "Post pytest results to the workflow summary",
  "main": "src/main.js",
  "scripts": {
    "build": "ncc build --license=LICENSE src/main.js",
    "format": "prettier --write ."
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/pmeier/pytest-results-actions.git"
  },
  "keywords": [
    "pytest",
    "results",
    "actions",
    "summary"
  ],
  "author": "Philip Meier",
  "license": "BSD 3-Clause",
  "dependencies": {
    "@actions/core": "^1.10",
    "@actions/glob": "^0.4",
    "fast-xml-parser": "^4.1"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.36",
    "prettier": "^2.8"
  }
}
